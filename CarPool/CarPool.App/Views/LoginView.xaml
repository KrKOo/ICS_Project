<UserControl x:Class="CarPool.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarPool.App.Views"
             xmlns:texts="clr-namespace:CarPool.App.Resources.Texts"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    
    <Grid VerticalAlignment="Center">
        <Grid.RowDefinitions >
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="40" HorizontalAlignment="Center" Style="{StaticResource BasicTopBottomMargin}">
            <Bold>Welcome back!</Bold><LineBreak/>
            Please enter your E-mail
        </TextBlock>

        <TextBox Grid.Row="1" Width="500" Style="{StaticResource BasicTopBottomMargin}" />

        <Button Grid.Row="2" x:Name="EnterLoginButton" Style="{StaticResource IntroButton2}" Content="{x:Static texts:LoginResources.Button_Content}"/>
    </Grid>

</UserControl>
